struct Test {
    const float nochange = 69420;

    int x;
    int y = 32;

    mkfn _(int xval) void {
        this.x = xval;
    }
}

const Test x = make Test(43);
println("X: {" + toStr(x.x) + ", " + toStr(x.y) + ", " + toStr(x.nochange) + "}");
