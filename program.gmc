struct Test {
    var int x;
    var int y = 74;
    const float nochange = 69420;

    int x;
    int y = 32;
}

struct Strange {
    int z = 54;
}

const Test x = make Test;
println("X: {" + toStr(x.x) + ", " + toStr(x.y) + ", " + toStr(x.nochange) + "}");
